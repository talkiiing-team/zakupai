import { DatasetNode } from '@/features/graph-designer/nodes/dataset-node';
import { IncreaseTargetNode } from './increase-target-node';

export const nodeTypes = {
    dataset: DatasetNode,
    inc_target: IncreaseTargetNode,
};
